/**
 * wills pipeline — scaffold.
 *
 * The actual pipeline (classify → extract → review → compose) will be added
 * here as the module is implemented. Schema lives in @law/schema.
 */
export const __MODULE__ = "wills" as const;
